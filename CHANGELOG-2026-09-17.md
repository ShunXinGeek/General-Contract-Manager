# 更新日志

## v2026.09.22（2026-09-22）

本版本完善“管理助手”的话题浏览、自动命名、回复收藏和笔记管理能力，并保持本地优先与现有 Firebase 增量同步架构。

### 话题与档案库

- 修复档案库中点击话题名称无响应的问题；归档话题现在可在中间栏打开并查看完整聊天记录。
- 归档预览采用只读模式，保留复制和收藏操作；恢复话题后可继续对话、重新生成、分支或删除消息。
- 归档预览与当前活动话题分离，查看档案不会改写活动话题的草稿、上下文断点或消息记录。

### AI 自动话题命名

- 用户收到首条完整模型回复后，使用生成该回复的同一模型自动生成简体中文话题名称。
- 自动名称最多 13 个汉字；模型调用失败或返回无效内容时保留问题摘要作为降级名称。
- 用户手动重命名后，自动命名结果不会覆盖用户名称；分支话题继续使用递增编号命名。
- 标题生成使用独立的短请求，不发送完整话题历史、附件正文或合同知识库内容。

### 回复收藏与笔记本

- 在模型回复操作栏新增“收藏”按钮，可将回复正文保存为独立笔记副本。
- 在“档案库”上方新增“笔记本”入口，支持笔记列表、搜索、正文展示和标题/正文编辑保存。
- 相同话题中相同版本的回复不会重复收藏；重新生成后内容变化的回复可以作为新的笔记保存。
- 笔记与原话题相互独立，原回复删除或重新生成不会影响已收藏内容。
- 笔记采用 LocalForage 本地优先保存，并通过用户级 Firebase 子集合进行增量同步；离线收藏和编辑会在联网后补传。
- 笔记内容继续使用安全 Markdown 渲染，不保存渲染后的 HTML。
- 笔记本左侧每条笔记新增三点操作菜单，可通过“取消收藏”删除笔记；删除状态会保留并参与云端增量同步，避免已删除笔记在其他设备重新出现。
- 笔记编辑新增“源码”“预览”和“对比”三种模式；源码和预览模式分别铺满编辑区域，对比模式左右分栏显示 Markdown 源码与渲染结果。
- 对比模式支持源码编辑时实时更新预览，并按滚动比例同步两侧内容位置。

### 测试与工程检查

- `npm test`：通过，新增笔记标题、重复收藏、回复版本变化和本地恢复测试。
- `npm run check`：通过，57 个 JavaScript 文件及项目清单/锁文件检查通过。
- `node scripts/verify-assistant-browser.js`：通过，覆盖同模型自动命名、13 字限制、档案库预览、收藏、笔记源码/预览/对比模式、实时渲染、滚动同步、取消收藏、刷新恢复及既有管理助手流程；真实供应商请求数为 0。
- `git diff --check`：通过。

本次更新仅提交至 GitHub，未执行 Netlify、Firebase 规则或生产数据部署。

## v2026.09.21（2026-09-21）

本版本完成国内外大模型的统一配置、协议适配与受控网关升级，重点解决不同服务商的地址、端口、思考参数、流式协议和浏览器跨域调用差异。

### 多供应商模型配置

- 新增供应商注册表，支持 OpenAI、Gemini、智谱 AI、Kimi 中国/国际、DeepSeek、通义千问北京/新加坡/美国，以及部署者授权的自定义 OpenAI 兼容服务。
- 设置页新增供应商/区域、调用协议、Base URL、连接超时和流式空闲超时字段；端口统一写在 URL 中，避免独立端口字段与地址不一致。
- 支持 OpenAI Chat Completions、OpenAI Responses 和 Gemini OpenAI 兼容协议；Responses 流会转换为现有统一 SSE 事件格式。
- 思考参数按模型能力适配：通义使用 `enable_thinking`，Gemini 使用 `reasoning_effort`，其他兼容模型使用对应的 `thinking` 结构；推理模型不再接收不兼容的采样参数。
- AI 配置快照由 v2 升级为 v3；旧版单模型、旧云端快照和旧模型列表会自动补齐供应商、协议及超时字段，并保留旧字段兼容读取。

### 受控网关与检索调用

- `/api/ai` 统一转发聊天请求，新增 Gemini 官方兼容地址和 OpenAI Responses 转换，同时保留原有国内模型路径及错误诊断。
- Embedding 与 Rerank 迁移至同源 `/api/retrieval`，浏览器不再直接向第三方服务商发送 API Key，消除跨域和鉴权格式差异。
- 聊天、Embedding 和 Rerank 目标均采用 HTTPS、域名、端口和路径精确校验；自定义目标需由部署者通过 `AI_ALLOWED_ENDPOINTS` 或 `AI_ALLOWED_RETRIEVAL_ENDPOINTS` 明确授权，不提供任意 URL 代理。
- Service Worker 缓存版本升级至 v7，确保新适配脚本能够刷新到客户端。

### 文档与验证

- 更新 README，补充供应商地址、协议、端口、自定义白名单和部署说明。
- `npm test`：通过，覆盖云同步、旧配置迁移、AI 传输、Responses 流、检索网关和既有 RAG 回归。
- `npm run check`：通过，53 个 JavaScript 文件及项目清单/锁文件检查通过。
- `node scripts/verify-assistant-browser.js`：通过，覆盖管理助手、话题、附件、检索索引、离线冷启动和配置恢复；使用 Mock，真实供应商请求数为 0。
- 未执行 Netlify、Firebase 规则或生产数据部署；真实供应商连通性和用量仍需使用用户提供的测试凭据另行验收。

## v2026.09.20（2026-09-20）

本版本完善“管理助手”的话题工作区，并增加从大模型回复继续探索的独立分支能力。

### 管理助手附件

- 上传入口现支持一次选择多个 TXT、Markdown（`.md`/`.markdown`）、PDF 与 DOCX 文件；不再接受无法可靠解析的旧版 DOC。
- TXT/Markdown 使用严格编码解码；PDF 由本地 PDF.js 解析，DOCX 在浏览器 Worker 中由 Mammoth 解析。原始文件字节不会上传或持久化，扫描件与受保护文件会明确提示无法提取文本。
- 附件在发送前显示独立状态与删除操作，不再覆盖用户正在编辑的问题；单文件、总大小、数量、提取长度、解析时长及请求大小均有明确限制，拒绝静默截断。
- 聊天记录主消息只保存附件摘要；正文分别保存在本地和 Firebase 的附件子集合。删除消息会写入覆盖式墓碑，避免保留此前正文。
- 知识库检索/Embedding 仅接收用户问题，不接收附件全文；发送给聊天模型时也会把附件标为不可信的待分析证据，而非指令。

### 管理助手话题工作区

- 新增默认展开的“话题”侧边栏，支持新建、切换、搜索、重命名、置顶、拖拽排序、归档、恢复和永久删除。
- 顶部 Navigator 开关在管理助手中改名为“切换侧边栏”，支持窄屏抽屉、遮罩和 Esc 关闭。
- 话题列表仅显示话题名称；当前话题使用整行主题色背景区分，不再使用左侧标记。
- 每个话题独立保存聊天记录、草稿和上下文断点；旧的单一聊天记录会迁移为“历史对话”。
- 话题元数据与逐条消息按用户同步至 Firebase；离线修改进入待同步队列，网络恢复后自动补传。

### 回复分支

- 为每条大模型回复增加“分支”操作，可复制从话题开始到当前回复为止的完整记录。
- 新分支自动显示在来源话题上方并立即切换为当前话题，命名为“原名称（1）”“原名称（2）”等递增形式。
- 分支保留思考过程、引用校验、检索诊断和上下文断点等消息元数据，后续对话与原话题相互独立。
- 创建分支只执行本地复制和 Firebase 同步，不会额外请求大模型。

### 交互与布局

- 话题操作菜单支持点击外部区域或按 Esc 关闭。
- “发送”按钮移入输入框底部工具栏并靠右排列，与清空、终止上下文、引用条款及上传文档按钮保持同一行。
- 为消息操作按钮补充无障碍名称和分支矢量图标；分支创建期间提供禁用反馈。

### 测试与工程检查

- `npm test`：通过，包含话题迁移、分支截断、连续编号、来源话题保护、离线队列及 Firebase 写入测试。
- `npm run check`：通过，47 个 JavaScript 文件及项目清单检查通过。
- `node scripts/verify-assistant-browser.js`：通过，覆盖分支创建、消息截断、连续分支、侧边栏交互及既有管理助手流程，无未捕获 JavaScript 错误。
- 附件浏览器回归覆盖多文件 TXT、Markdown、PDF、DOCX 的本地解析、发送正文注入与安全摘要展示；话题单测覆盖附件主消息/子集合分离及删除墓碑。
- 发布前已清理浏览器回归生成的 `output/` 临时目录；`.claude/`、规划记录等未纳入程序提交。

本次提交仅上传至 GitHub，未执行 Netlify、Firebase 规则或生产数据部署。

## v2026.09.17（2026-09-17）

本版本围绕合同原文基线、跨设备 AI 配置同步、离线使用和回归验证进行了修复与加固。

### 功能与架构改进

- **保留 AI 配置云端同步**：登录同一 Firebase 账号的新设备，可恢复完整的 AI 模型列表、当前选中模型、Embedding 配置、Rerank 配置及系统提示词。
- **AI 配置合并策略集中化**：增加版本号和修改时间，避免新设备的空白默认配置覆盖云端已有配置；兼容旧版仅保存单个模型的云端数据。
- **合同原文基线持久化**：每个条款保存 `originalContent`，刷新页面、重新加载合同或从云端恢复后仍可进行准确的修改比较和原文还原。
- **渐进式迁移旧数据**：对没有原文基线的历史合同，以迁移时的当前内容建立基线，并保留迁移标记，避免误报历史修改。
- **离线资源本地化**：将运行时 CDN 依赖纳入 `vendor/`，并由 Service Worker 预缓存核心脚本，支持首次访问后在离线状态加载应用外壳。

### 问题修复

- 修复条款修改状态在刷新、云端恢复及编辑后显示不一致的问题。
- 修复“查看原文/当前版本”比较窗口的 HTML 安全处理，并改进还原后的保存与状态更新。
- 修复合同导入内容中带转义双引号及多行引号文本时的解析截断问题。
- 修复 Service Worker 缓存清理范围过宽及缓存写入未纳入生命周期等待的问题。
- 修复 `package.json` 与 `package-lock.json` 内容不一致的问题。

### 测试与工程检查

- 新增 Node 回归测试，覆盖原文基线、历史数据迁移、云端 AI 配置合并、模型增删改及导入解析。
- 新增离线 Shell 测试，覆盖核心脚本预缓存、离线导航回退、运行时缓存和缓存隔离。
- 新增 Playwright 浏览器冒烟测试，覆盖导入、编辑、比较、还原、搜索、翻译、AI 配置恢复、模拟 AI 请求及离线冷启动。
- 新增脚本语法检查和项目清单/锁文件一致性检查。

### 部署说明

1. 将本版本重新部署到 Netlify，并保留现有的 `FIREBASE_*` 环境变量。
2. 在原电脑打开新版本后执行一次“立即同步”，确保完整的 AI 配置快照上传到 Firebase。
3. 新电脑访问同一网址并登录同一 Firebase 账号，即可加载上述 AI 配置。

本方案按已确认的“保留现有 Key 同步”选择实施：API Key 会随 AI 配置快照保存在 Firebase 中，Base64 仅用于本地显示层面的混淆，不是加密。因此必须确保 Firebase Firestore 规则仅允许对应用户访问自己的配置数据；本次更新未修改线上规则或执行部署。

### 验证结果

- `node tests/run.js`：通过（云端同步、状态恢复、离线 Shell）。
- `node scripts/check.js`：通过（35 个文件语法检查及清单/锁文件一致性）。
- Playwright 浏览器冒烟测试：通过；无未捕获 JavaScript 错误。

## v2026.09.18（2026-09-18）

本版本完成多服务商 AI 通信适配，并对“管理助手”在同时启用思考模式和知识库时的合同依据检索、索引一致性与引用约束进行了优化。

### AI 通信与部署

- 新增 Netlify Edge Function `/api/ai`，由本站转发 AI 对话请求，避免浏览器直连不同服务商时出现跨域、连接关闭或浏览器拦截问题。
- 支持 DeepSeek、通义千问、智谱 GLM、Kimi 以及其他 OpenAI 兼容接口；通过官方域名和聊天接口路径白名单限制转发目标，不提供任意 URL 代理。
- 按模型能力适配思考参数：千问使用 `enable_thinking`，DeepSeek、智谱和可切换的 Kimi 模型使用 `thinking.type`；固定思考模型不会发送不支持的关闭参数。
- 保留流式输出、思考内容、停止生成、重新生成和取消请求功能；Embedding 与 Rerank 的原有调用路径不变。
- Edge Function 不记录或缓存 API Key、合同内容及模型响应，并增加请求大小、超时、响应大小和频率限制。

### 问题定位与修复

- 将服务商 HTTP 400 的模糊提示细分为模型不存在、上下文超限和思考/输出参数不兼容等可操作提示，同时避免回显服务商原始响应中的密钥或请求内容。
- 确认 DeepSeek 当前 Model 字段必须填写官方模型 ID。`DeepSeek V4.1 Flash` 是展示名称，正确配置应使用 `deepseek-flash`；用户完成修改后测试连接和管理助手均运行正常。
- `ERR_BLOCKED_BY_CLIENT` 的 Firestore `Listen/Write` 终止请求与 AI 请求无关，不作为本次 AI 修复目标。

### 合同标签页引用语言状态

- 修复新增合同标签页右侧栏缺少“原/译”按钮的问题；所有合同标签页现在统一显示“原/译”和“简/繁”按钮。
- 普通条款引用与跨合同引用均支持相同的原文/译文切换逻辑；选择的显示模式会持续应用到后续打开的引用条款。
- 为每个合同标签页分别保存原文/译文及简体/繁体偏好；切换合同标签页后恢复对应偏好，新标签页默认显示英文原文和简体中文。
- “简/繁”仅在译文状态下改变右侧栏内容，同步翻译模式仍支持简繁切换。

### 管理助手：知识库检索与输出约束

- 新增合同身份解析，区分 GCC/SCC、编号、字母后缀及子条款；裸编号存在歧义时明确提示，不再默认归入 GCC。
- 明确条款编号走精确取回；综合问题合并当前正文关键词和有效向量候选，并在可用时依据合同身份、标题及正文重排。交叉引用只用于补齐可信关系，不再因单次命中停止联合检索。
- 只有与已知原文匹配的静态 GCC/SCC 关系才参与自动补齐；未知版本仅根据当前 SCC 原文中明确的修改语句建立关系。无法确认时保留为待核，不推断“没有修改”。
- 追问仅从有效用户会话恢复指代；重新生成不会引入之后的会话内容；“终止上下文”继续生效。
- 设置候选数量、主题数、引用层级和正文字符预算上限。正文预算不足、检索歧义或服务降级时会明确提示证据缺口，而不将未检出等同于合同不存在。
- 统一知识库回答规则：区分合同原文、解释、用户事实、条件性适用判断和待核事项；核对主体、触发条件、期限、程序、例外与后果。历史助手回答不作为合同证据。
- 回答完成后自动检查条款合同身份、直接引文和已提供的修改条款引用；必要时最多进行一次同模型定向修正，仍无法修正时显示具体核对提示。
- 向量索引新增模型、服务空间、维度、正文及标题指纹校验。缺少元数据、资料变更、模型/空间不匹配或维度异常的旧记录会保留但不参与语义检索，并提示用户主动更新索引。

### 测试、评测与边界

- 新增64条有来源依据的检索样本，预先固定为32条校准集和32条保留集；新增离线评测、受控在线评测和管理助手隔离浏览器验证脚本。
- 离线保留集的最低必需依据命中从旧流程的27/41提升至41/41，整题通过从20/32提升至32/32；标签是最低必需依据，不代表穷尽相关性或精确率。
- `node tests/run.js` 通过云同步、状态恢复、离线外壳、AI 传输及检索回归五套测试；`node scripts/check.js` 通过45个文件语法检查与清单一致性检查。
- 隔离浏览器验证覆盖思考与知识库开关、SCC 身份、条款跳转、追问、重新生成、上下文截断、一次修正、停止、刷新，以及 IndexedDB 索引构建/导入/失效/导出；未使用真实服务商凭据或生产数据。
- 未运行真实聊天、Embedding 或 Rerank 服务的对比，因此不以离线结果宣称真实模型答案质量、延迟、成本或语义效果已完成验收。受控在线评测需要调用方另行提供私有配置，并对答案逐项复核原文。

### 验证与发布

- `node tests/run.js`：通过，包含多服务商请求体、思考模式、流式 SSE、取消、错误诊断和既有云同步/离线回归测试。
- `node scripts/check.js`：通过，包含脚本语法及项目清单检查。
- 独立 Playwright 浏览器验证：通过，覆盖多服务商连接测试、模型错误提示、思考输出、重新生成、停止生成和刷新后配置恢复。
- 本次引用语言状态修复已完成本地浏览器验证：普通合同标签页显示两个按钮，并验证原文/译文、简体/繁体及连续条款导航状态保持。
- 已提交并推送至 GitHub：`d18feaa`（统一 AI 转发与多服务商适配）、`95a9f91`（服务商模型/参数错误诊断）。GitHub 连接的 Netlify 将自动部署最新提交。
- 本次本地检索优化尚待随当前提交推送；未单独执行 Netlify、Firebase 规则或生产数据变更。
