# 云同步问题修复计划与线程交接

生成时间：2026-09-13

当前状态：修复方案已实施并完成本地自动化测试、构建检查及浏览器刷新恢复验证。真实 Firebase 跨设备验证仍需在 Netlify 配置环境变量并更新 Firestore 规则后执行。

## 实施结果（2026-09-13）

- 编辑器现会捕获普通文本、加粗、斜体、下划线、颜色、高亮、备注和清除格式产生的正文变化，并记录条款 `modifiedAt`。
- 编辑内容会写入主合同 IndexedDB 快照；启动时兼容旧格式数据和旧自动保存补丁，并恢复当前合同。
- “立即同步”会先持久化本地编辑，再创建云端独有合同，按条款时间合并，最后上传当前完整合同快照。
- “上传本地数据”会完整写入本地快照，成功后再清理云端旧文档；错误会传递到界面，不再误报成功。
- “下载云数据”会用云端完整快照替换本地、恢复当前合同并立即持久化，同时兼容旧补丁格式。
- 已登录用户打开页面后会等待本地恢复完成，再自动执行一次同步。
- Netlify 构建现可从六个 `FIREBASE_*` 环境变量生成公共运行时配置，新设备不再依赖该浏览器的 localStorage 配置。
- README 中的 Firestore 规则已补充子集合授权；这是当前分批云存储格式正常读写所必需的配置。

已完成验证：

- `npm test`：通过，覆盖空设备恢复、云端较新正文覆盖本地原文、旧补丁不覆盖较新快照、上传错误向上抛出等路径。
- `npm run build`：通过；本机未设置 Netlify 环境变量，因此生成空运行时配置并输出预期警告。
- 改动 JS 文件 `node --check`：通过。
- `git diff --check`：通过。
- 浏览器实测：导入示例合同、编辑正文、加粗、等待自动保存并刷新，编辑内容、格式和当前合同均成功恢复。

待部署后验证：在 Netlify 设置 Firebase 环境变量、发布 README 中更新后的 Firestore 规则，并使用两个独立浏览器环境登录同一账号完成真实跨设备同步测试。

## 目标

修复合同编辑内容无法跨设备同步的问题。最终效果应为：用户在 Netlify 部署的网站中导入合同并进行正文编辑、添加高亮、添加备注、加粗、修改字体颜色等操作后，点击云同步或上传本地数据，同一账号在另一台电脑打开该网站时可以看到完整一致的编辑结果。

## 当前架构理解

项目是静态前端应用，Netlify 只负责托管 HTML/CSS/JS。合同数据主要保存在浏览器本地 IndexedDB/LocalForage 中，云同步路径实际是 Firebase Auth + Firestore，而不是 Netlify 本身，也不是 Cloudflare。

关键模块：

- `index.html`：页面结构和脚本入口。
- `js/app.js`：应用启动、合同列表、当前合同状态和 IndexedDB 初始化。
- `js/editor.js`：中间栏正文编辑、高亮、备注、格式化和本地自动保存。
- `js/cloud-storage.js`：Firebase 登录、Firestore 读写、立即同步、上传本地数据、下载云数据。
- `js/firebase-config.js`：Firebase 配置初始化。

## 用户复现路径

1. 在电脑 A 打开 Netlify 部署的网站。
2. 导入合同文件。
3. 在新生成标签页的中间栏编辑内容，例如添加高亮、备注、加粗、改字体颜色。
4. 点击“云备份”中的“立即同步”和“上传本地数据”。
5. 在电脑 B 打开同一个 Netlify 网站。
6. 电脑 B 未看到电脑 A 做过的编辑。

## 已确认问题

### 1. 编辑识别范围过窄

位置：`js/editor.js`，`hasUserModifications` 附近。

当前逻辑主要识别这些内容：备注 span、背景色、非 CSS 变量颜色、`<b>`、`<font>`。这会漏掉普通文本修改、斜体、下划线、清除格式等编辑。漏掉的编辑不会进入 `contract_modifications`，跨设备自然无法按修改记录还原。

静态复现结果：

- 普通文本修改：未识别。
- 加粗：可识别。
- 高亮：可识别。
- 备注：可识别。
- 斜体：未识别。
- 下划线：未识别。

### 2. 云端数据应用顺序错误，新设备可能先崩溃

位置：`js/cloud-storage.js`，`applyCloudDataToLocal` 附近。

当前逻辑先把云端修改应用到已有本地合同，再创建云端存在但本地不存在的合同。新设备没有本地合同或没有当前合同时，会先访问不存在的本地对象，导致流程中断。

静态复现结果：

```text
Cannot read properties of undefined (reading 'data')
```

### 3. 已有本地合同不会被云端完整内容覆盖

位置：`js/cloud-storage.js`，创建/合并云端合同数据附近。

当前逻辑只有在本地合同内容为空时才应用云端 `contract_data`。如果电脑 B 重新导入了同一份基础合同，本地已有原始内容，云端完整编辑内容不会覆盖本地内容，只能依赖修改记录补丁；而修改记录本身又会漏掉很多编辑。

静态复现结果：本地已有基础合同时，云端完整内容没有替换本地原始内容。

### 4. 同步时优先保留旧云端快照

位置：`js/cloud-storage.js`，`syncWithCloud` 的 `contract_data` 赋值附近。

当前逻辑类似：

```js
contract_data: cloudData?.contract_data || allContractDataForSync
```

一旦云端已经存在 `contract_data`，后续普通同步会继续保留旧云端快照，而不是上传当前本地完整内容。这会造成云端主体数据长期停留在旧版本。

### 5. 上传失败会被显示成成功

位置：`js/cloud-storage.js`，`forceUploadToCloud` 和 `handleForceUpload` 附近。

`forceUploadToCloud` 捕获错误后返回 `false`，但调用方没有根据返回值决定提示文案，仍可能显示上传成功。这会误导用户，以为数据已经进入云端。

### 6. 自动保存和启动读取不是同一条持久化路径

位置：`js/editor.js` 的自动保存逻辑，以及 `js/app.js` 启动读取逻辑。

编辑器自动保存写入 `AUTO_SAVE_KEY`，但应用启动主要从 `CONTRACTS_DB_KEY` 读取。云端数据应用后也没有稳定地持久化回主合同存储。这会导致刷新页面或换设备后，编辑状态无法可靠恢复。

### 7. Firebase 配置对新设备不友好

位置：`js/firebase-config.js`。

文件里是占位配置，真实配置依赖当前浏览器的 `localStorage`。如果新设备没有先手动配置 Firebase，就无法初始化 Firebase，也就无法登录和拉取云端数据。这是跨设备同步的前置阻断点。

## 根因总结

问题不是 Netlify 静态托管导致的，也不是单一按钮失效。根因是本地编辑状态、云端完整快照、云端修改补丁和应用启动读取路径之间没有统一的数据源和同步协议。

具体表现为：

- 编辑采集不完整，很多正文变化没有进入云端补丁。
- 云端完整快照没有在普通同步中持续更新。
- 下载云端数据时，先应用补丁再创建合同，导致新设备路径不稳定。
- 本地已有基础合同时，云端完整编辑内容被忽略。
- 失败提示不可靠，用户无法判断云端是否真的写入成功。
- Firebase 配置依赖单机 localStorage，无法天然跨设备工作。

## 推荐修复方向

采用一个明确的数据源模型：以完整合同数据 `contracts` 为主数据源，每个条款/段落保存当前 `content`、`modifiedAt`，必要时保存 `deletedAt` 或删除标记。格式化、高亮、备注、正文文本修改都应反映到条款当前内容中，而不是只依赖格式标签白名单来判断是否需要同步。

建议原则：

1. 编辑发生时显式标记脏数据，并捕获当前 DOM 内容到内存状态。
2. 同步前先执行“DOM -> 内存合同状态 -> IndexedDB”的落盘，再上传云端。
3. 普通同步按合同和条款的更新时间合并本地与云端数据。
4. 上传本地数据表示本地完整替换云端。
5. 下载云数据表示云端完整替换本地。
6. 下载时先创建/补齐合同，再应用书签、当前合同和渲染状态。
7. 所有上传/下载错误必须向调用方抛出或返回明确失败，UI 只在真实成功后显示成功。
8. Firebase 配置应通过部署环境或构建产物提供，不能只依赖单台电脑的 localStorage。

## 建议修改范围

预计只需要触及这些文件：

- `js/editor.js`：编辑捕获、脏标记、正文内容落盘。
- `js/cloud-storage.js`：云端 payload、上传/下载/普通同步、错误提示、合并策略。
- `js/app.js`：启动读取、本地合同状态恢复、云端数据应用后持久化。
- `js/firebase-config.js`：Firebase 配置加载方式。

可选：

- 添加小型测试脚本或测试用例，用于验证合并和编辑识别逻辑。
- 实施完成后再更新 `README.md` 的同步说明。

不要在本次问题之外重构无关 UI、样式、导入导出、搜索等功能。

## 建议实施顺序

1. 梳理当前合同数据结构，确认合同 key、条款 id、正文 content、书签、备注和当前合同状态的字段关系。
2. 新增或改造统一保存函数：从编辑器 DOM 捕获当前合同内容，并写入主合同状态和 IndexedDB。
3. 调整编辑器事件：普通文本输入、格式化按钮、高亮、备注、颜色、清除格式都应触发保存和脏标记。
4. 改造云端上传：上传当前完整合同数据，不复用旧云端 `contract_data` 作为优先值。
5. 改造云端下载：先 hydrate/create 合同，再应用云端内容、书签、当前合同，并持久化到 IndexedDB。
6. 改造普通同步：基于 `modifiedAt` 合并本地和云端条款；冲突时选择较新内容，必要时保留后续冲突提示扩展点。
7. 改造“上传本地数据”和“下载云数据”：语义明确为强制覆盖，并修正成功/失败提示。
8. 处理 Firebase 配置：为 Netlify 部署提供稳定配置来源，并确认新设备无需手动 localStorage 配置即可登录。
9. 补充验证脚本或手工浏览器验证。

## 验收标准

修复完成后至少应满足：

- 全新浏览器环境登录后，可以从云端恢复合同列表、当前合同和正文编辑内容。
- 另一台电脑重新导入同一基础合同时，云端较新的编辑内容不会被本地原文静默覆盖。
- 普通文本修改、高亮、备注、加粗、字体颜色、斜体、下划线、清除格式等编辑可以跨设备显示。
- 页面刷新后，编辑内容仍存在。
- “立即同步”不会保留旧云端快照导致新编辑丢失。
- “上传本地数据”真实成功后才显示成功；失败时显示失败原因。
- “下载云数据”可以明确用云端数据恢复本地。
- 不破坏导入、合同切换、书签、搜索、导出等现有功能。

## 验证清单

代码层面：

- 对改动过的 JS 文件运行语法检查，例如 `node --check`。
- 对新增测试或验证脚本运行对应命令。
- 运行 `git diff --check` 检查空白和补丁格式。

场景层面：

- 使用一个浏览器配置文件导入合同并完成多种编辑，然后上传。
- 使用另一个干净浏览器配置文件登录同一账号，确认完整恢复。
- 在第二个浏览器先导入同一基础合同，再同步，确认云端编辑仍能覆盖或合并。
- 模拟上传失败，确认 UI 不显示成功。
- 刷新页面后确认当前编辑仍可恢复。

## 已有静态复现证据

已通过只读 Node `vm` 方式验证过以下行为，没有创建测试文件：

```text
新空设备 applyCloudDataToLocal 报错：
Cannot read properties of undefined (reading 'data')

已有基础合同 + 云端完整内容：
本地内容仍为 original，而不是云端 edited。

完全匹配的加粗修改：
可以应用为 <b>formatted</b>。

hasUserModifications：
plain=false
bold=true
highlight=true
note=true
italic=false
underline=false
```

## 当前工作区状态备注

此前检查结果：

- 当前分支 `main` 与 `origin/main` 已同步。
- 本地只有未跟踪的 `.claude/` 目录，不属于本次任务，不应触碰。
- 截至创建本文档前，程序代码尚未修改。

## 新线程建议开场提示

```text
请读取 CLOUD_SYNC_FIX_PLAN.md，按其中范围实施云同步修复。先检查当前工作区差异，遵守最小改动原则；完成后运行文档中的验证清单，并说明哪些项目已验证、哪些由于缺少线上凭证或环境只能给出替代验证。
```
